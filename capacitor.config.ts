import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vibrant.maikerudev',
  appName: 'Vibrant',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
